#ifndef FUNCTION_LIKE_MACRO_H
#define FUNCTION_LIKE_MACRO_H

/*
 * Macro: 3-function_like_macro.h
 * Detail: Header file that defines a function-like macro ABS(x)
 *       that calculated the absolute value of a num x.
 */

#define ABS(x) ((x < 0) ? -(x) : (x))
#endif
