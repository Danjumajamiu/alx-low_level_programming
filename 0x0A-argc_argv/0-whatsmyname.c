#include "main.h"
#include<stdio.h>

/**
 * main - will print the name of my program
 * @argc: command line of arguments count
 * @argv: array name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
